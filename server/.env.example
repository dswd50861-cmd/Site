# Server configuration
PORT=4000
JWT_SECRET=change-me
# If empty, an in-memory database will be used via pg-mem
DATABASE_URL=postgres://user:pass@localhost:5432/operations
# Frontend origin for CORS
CORS_ORIGIN=http://localhost:5173
